# 宇测科技 PLMS

## Logo 替换说明

请将贵司的 `公司logo2.png` 复制到本目录，重命名为 `logo.png`，并在 `index.html` 中更新 favicon 引用：

```html
<link rel="icon" type="image/png" href="/logo.png" />
```

## 当前 Logo

`logo.svg` 为占位 SVG Logo（蓝橙渐变 + YUCE 字样），与系统主题色一致。
